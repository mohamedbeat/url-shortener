info:
  name: create link
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/api/links"
  body:
    type: json
    data: |-
      {
        "title":"unnecessairy",
        "url":"https://mohammeddotgo.netlify.app/none"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
