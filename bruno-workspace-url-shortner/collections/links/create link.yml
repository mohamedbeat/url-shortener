info:
  name: create link
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/api/links"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
