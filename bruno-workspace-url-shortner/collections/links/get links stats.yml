info:
  name: get links stats
  type: http
  seq: 9

http:
  method: GET
  url: "{{api_url}}/api/links/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
