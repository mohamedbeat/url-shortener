info:
  name: check if slug exists
  type: http
  seq: 8

http:
  method: GET
  url: "{{api_url}}/api/links/checkSlug/linkfsd"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
